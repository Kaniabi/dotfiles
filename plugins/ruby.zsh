# RUBY programming language

if ( $IS_MAC ) then
  export PATH="$HOMEBREW_PREFIX/opt/ruby/bin:$HOMEBREW_PREFIX/lib/ruby/gems/3.4.0/bin:$PATH"
else
  TODO Setup path for ruby on non-macOS systems
fi
INSTALL_CMD ruby
